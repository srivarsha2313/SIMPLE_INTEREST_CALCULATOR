# SIMPLE_INTEREST_CALCULATOR
FINAL PROJECT

SIMPLE INTEREST CALCULATOR

📌 Project Overview This project is a Simple Interest Calculator built to demonstrate basic programming concepts and GitHub repository management. It calculates the simple interest based on user inputs for principal amount, rate of interest, and time period.

Formula used: SI = (P * R * T) / 100 where: P = Principal amount R = Rate of interest (per annum) T = Time (in years)

🚀 Features

Accepts user input for principal, rate, and time.
Calculates simple interest using the standard formula.
Displays the result clearly to the user.
Lightweight and beginner-friendly project.
Installation & Usage

Clone the repository: git clone https://github.com/your-username/github-final-project.git

Navigate to the project directory: cd simple-interest-calculator

Run the script (example in Bash/Python depending on your implementation): bash interest_calculator.sh
